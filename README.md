TuteDude – Demo Page
This project is a demo HTML webpage created as part of the **TuteDude – 01: HTML Learning Demo Page** assignment.
The purpose of this project is to demonstrate a strong understanding of **HTML semantics, structure, and commonly used HTML elements** by building a well-organized and accessible webpage for a fictional learning platform.
 Project Overview
The webpage is structured using semantic HTML elements and includes multiple sections such as a header, main content, sidebar trivia, and footer.
It demonstrates the correct usage of:
- Headings and text formatting
- Semantic layout tags
- Tables (including nested tables)
- Media elements
- Forms and user input elements
Page Structure
The webpage uses the following semantic elements:
- `<header>` – Welcome message, navigation menu, and hero image  
- `<main>` – Core content of the webpage  
- `<section>` – About, Menu, Trivia, Extras, and Feedback  
- `<article>` – Platform description  
- `<aside>` – Trivia content  
- `<footer>` – Contact details and copyright information  
Features Implemented
- **Highlighted heading** using `<mark>`
- **Text emphasis** using `<strong>` and `<em>`
- **Navigation menu** using `<nav>`
- **Hero image** with `<figure>` and `<figcaption>`
- **Abbreviations** using `<abbr>`
- **Time representation** using `<time>`
- **Quotation block** using `<blockquote>` and `<cite>`
- **Complex tables** with `<thead>`, `<tbody>`, and `<tfoot>`
- **Nested tables** inside menu items
- **Collapsible trivia section** using `<details>` and `<summary>`
- **Audio and video embedding**
- **Feedback form** using:
  - `<form>`
  - `<fieldset>` and `<legend>`
  - Text, email, select, textarea, and submit button
- **Contact information** using `<address>`
-  🚀 Live Project Link
GitHub Pages Deployment:  
https://voralucky5-lang.github.io/nestjs-fundamentals/
